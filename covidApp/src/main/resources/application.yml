server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Covid
    username: postgres
    password: fullstack
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: false

